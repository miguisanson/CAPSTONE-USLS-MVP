{
  "name": "capstone-usls-python-demo",
  "private": true,
  "version": "0.2.0",
  "description": "Python demo for the USLS Graduate School lifecycle monitoring system",
  "scripts": {
    "install:python": "python -m pip install -r requirements.txt",
    "seed": "python app.py --seed",
    "start": "python app.py",
    "dev": "powershell -ExecutionPolicy Bypass -File ./run_demo.ps1"
  }
}
